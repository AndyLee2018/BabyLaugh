# 有了IP地址，为啥需要MAC地址：
	|- MAC地址是联网设备的唯一标识，IP地址是动态可变的；
	|- 规定：网络是分层的，网卡处于数据链路层，需要用MAC地址与其它设备通信；
	|- 网络多种多样，可不仅仅是TCP/IP , 只用IP地址无法与那其他网络通信；
# ARP:地址解析协议：
	|- 解析获取MAC地址；
	|- 解析过程：通过广播形式将寻址信息（信息中包含本机IP,本机MAC地址，对方主机IP地址）发送出去，局域网内的主机们收到信息后通过信息中的IP判断是不是找自己，确认是自己的主机返回一个APR应答返回自身MAC地址；